export type Person = {
  name: string;
  role: string;
  image: string;
  description: string;
};

export type Program = {
  title: string;
  description: string;
  image: string;
};

export type StudentProject = {
  title: string;
  author: string;
  date: string;
  images: string[];
  description: string;
  preview: string;
};
